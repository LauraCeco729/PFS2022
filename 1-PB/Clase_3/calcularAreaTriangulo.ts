ejemplo:
import * as rls from 'readline-sync';

function calcularArea (numero: number) 



let base : number = rls.questionInt("Ingrese un número: "); ;
let altura : number = rls.questionInt("Ingrese un número: "); 

if (calcularArea = base*altura/2)


// function calcularAreaTriangulo(base : number, altura: number){

    console.log(base * altura / 2);

}

calcularAreaTriangulo(1,2);
calcularAreaTriangulo(2,4);
asi ?// 